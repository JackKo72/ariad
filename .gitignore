# Python
apps/api/.venv/
apps/api/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/
apps/web/.next/
apps/web/node_modules/

# Local data (never commit local SQLite DB, synthetic or not)
data/
*.db
*.sqlite3

# Debug artifacts (local-only, synthetic data mode)
debug_runs/

# Downloaded sherpa-onnx model weights (Phase D real ASR provider, GBs --
# never committed; each dev downloads their own, see README.md)
apps/api/models/

# Env
.env
.env.local

# Test artifacts
test-results/
playwright-report/
apps/web/coverage/
